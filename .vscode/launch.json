{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Windows Launch",
            // "program": "${workspaceFolder}/main.exe",
            "program": "${workspaceFolder}/build/Debug/RaysGame.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}